/**
 * Takes in 2 numbers, returns their sum
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */

const solution = (a, b) => {
  return  a + b;
};

test = solution(2, 5);

console.log(test);

module.exports = {
  solution,
};
